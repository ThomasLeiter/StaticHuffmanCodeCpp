#ifndef HUFFMAN_H
#define HUFFMAN_H

#include <string>

namespace huffman
{

    std::string encode(const std::string &);
    std::string decode(const std::string &);

} // namespace huffman

#endif // HUFFMAN_H